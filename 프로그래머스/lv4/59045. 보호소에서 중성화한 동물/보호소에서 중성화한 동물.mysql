SELECT
    INS.ANIMAL_ID,
    INS.ANIMAL_TYPE,
    INS.NAME
FROM
    ANIMAL_INS INS
INNER JOIN
    ANIMAL_OUTS OUTS
    ON INS.ANIMAL_ID = OUTS.ANIMAL_ID
WHERE
    NOT (INS.SEX_UPON_INTAKE LIKE '%SPAYED%' or INS.SEX_UPON_INTAKE LIKE '%NEUTERED%')
    AND 
    (OUTS.SEX_UPON_OUTCOME LIKE '%SPAYED%' OR OUTS.SEX_UPON_OUTCOME LIKE '%NEUTERED%')